import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.visitor.app',
  appName: 'visitorapp',
  webDir: 'dist/visitor-management-app/browser'
};

export default config;
